import React from 'react';
import Logo from './Logo'

export const Home = () => {
    return (
        <div>
            <Logo />
            hello from HOME
        </div>
    );
};

export default Home;