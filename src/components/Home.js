import React from 'react';
import Logo from './Logo'

export const Home = () => {
    return (
        <div>
            <Logo large />
            hello from HOME
        </div>
    );
};

export default Home;